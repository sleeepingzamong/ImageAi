# ImageAi 계획서

## AI 구현 방법
-------------------
-머신 러닝은 사람이 직접 특징을 입력해야 하고 픽셀 그대로의 처리가 어려우며 복잡한 이미지에 한계가 있다 -> 이미지 분류에 적합한 방식이 아님 ❌

-딥 러닝은 픽셀 그대로의 처리를 할 수 있고 공간 구조를 이해할 수 있음 -> 이미지 분류에 적합 ⭕

-이미지를 분류하는 Ai다 보니 픽셀이 중요해 일반 딥 러닝이 아닌 CNN을 사용하기로 결정

--------------------------------------------------------------------------------------

## CNN?
-------------------
-이미지 전용 딥 모델

-지역적 특징을 효율적으로 학습하기 위해 합성곱 연산을 사용하는 딥러닝 모델

--------------------------------------------------------------------------------------

## CNN의 기본 구조(단계)
-------------------
📌 -Convolution(특징 추출) -> ReLU(비선형성) -> Pooling(크기 축소) -> 지금 까지 반복 -> Flatten(벡터 변환) -> Dense(분류) -> Output(최종 결과)

> ## 1. Convolution Layer (합성곱 층)
> 역할 : 합성곱 층이 필터를 사용해서 *국소적인 특징*을 추출
>
> 작동 원리 : 1000개의 사진을 분석하게 되면 1000개당 각각의 CNN단계들이 적용되고 그중 기준(가중치 값)은 모두 공유된다.  가중치 값은 중요한 특징이면 크고 덜 중요하면 작다.
> 
>             EX) 1번 필터 : 귀  2번 필터 : 꼬리 ...
>
> 결과 : 필터(가중치 값의 행렬)가 찾은 특징에 따라 양수 , 음수 , 잡음이 결과 값으로 나옴

> ## 2. Activation Function (ReLU)
> 역할 : 비선형성 부여 , 모델 구체화 , 중요한 값만 살리기
>
> 작동 원리 : 이 단계에서 양수는 그대로 유지 , 음수는 특징을 찾지 못한 값이므로 0으로 변한다.
>
> 필요성 : 이 단계가 없으면 모델이 복잡한 패턴을 못 배움
>
> 결과 : 양수 -> 그대로 유지  음수 -> 0으로 변환

> ## 3. (Max)Pooling (요약하기)
> 역할 : 이미지에서 필터가 찾은 영역 중 가장 강하게 반응한 값만 남김
>
> 작동 원리 : 앞 단계에서 부여된 값이 가장 큰 곳만 남김
> 
> 필요성 : 크기 감소 -> 계산량 감소 , 위치 변화에 강함
>
> ⚠️주의!! : 전체 이미지에서 *하나*만 나오는게 아닌 필터마다 여러 개의 높은 값들이 추출됨


> ## 4. 지금까지의 단계들을 반복
> 역할 : 이미지들의 특징을 계층적으로 추출하여 분류 기준을 추상화하기 위함
>
> 작동 원리 : 단계마다 다른 특징들을 추출해서 하나의 개체의 조합을 명확히 함
> 
> 필요성 : 계산량을 낮추며 계층적으로 개체에 대한 복잡한 조합들을 만듬


> ## 5. Flatten (벡터 변환)
> 역할 : 현재까지의 단계에서 2차원 구조로 추출된 특징값들을 1차원 구조(벡터)로 나열해놓음 
>
> 필요성 : 일반 신경망이 처리할 수 있게 만듬


> ## 6. Fully Connect (종합 판단) =일반 신경망
> 역할 : 지금까지 찾은 특징들을 종합해서 질문(앞 단계에서 완성된 조합에 대한 판단)
>
> 작동 원리 : 하나의 형태에 대한 가중치(필터)와 같은 형태에 대한 입력한 사진의 특징 값의 곱, 이러한 한 형태에 대한 가중치와 특징값의 곱연산이 이뤄진 여러개의 형태끼리 똑같이 합연산을 하여 점수를 계산한다
>
>         EX)점수 = 눈의 크기의 가중치 값 * 눈의 크기의 특징값 + 꼬리의 길이의 가중치 값 * 꼬리의 길이의 특징값

> ## 7. Output (결론)
> 역할 : 각 클래스에 대한 확률을 만듬
>
> 작동 원리 : 
> 
> 필요성 :
